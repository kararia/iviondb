{% extends '/layout.html.twig' %}

{% block body %}

<div class="container decklists">
    <div class="row">
        <div class="col-md-12">
            <h1>The Gracious Donators</h1>
            <p class="lead">IvionDB wouldn't be the same without them. Many, many thanks.</p>
            <p>To show our appreciation, the content of the site is free of ads for Donators.</p>
            <p>To become a Donator and help run the site, [URL TBD] </p>
            <ul>
                {% for donator in donators %}
                <li>
                    <a href="{{ path('user_profile_view', {user_id:donator.id,user_name:donator.username|e('url')}) }}" class="username {{ donator.faction }}">{{ donator.username }}</a>
                    <span class="glyphicon glyphicon-gift donator" title="IvionDB Gracious Donator"></span>
                </li>
                {% endfor %}
            </ul>
        </div>

    </div>
</div> <!-- .container -->

{% endblock %}
